@model PagedList.IPagedList<WebApplication2.Models.MemberCompanyInfo>
@using PagedList.Mvc;
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using(Ajax.BeginForm(
                            new AjaxOptions
                            {
                                HttpMethod = "post",
                                InsertionMode = InsertionMode.Replace,
                                UpdateTargetId = "memberinfo"

                            }
                            ))
{
   <label>MemberId:</label> <input type="search" name="MemberId" id="MemberId" /><br />
<label>Applicant Name:</label>  <input type="search" name="AppName" id="AppName" value="@ViewBag.currentAppName" /><br />
<label>Organization Name::</label>   <input type="search" name="orgName" id="orgName" /><br />
    <input type="submit" name="submit" value="Search" />
}
    @Html.Partial("_members",Model)