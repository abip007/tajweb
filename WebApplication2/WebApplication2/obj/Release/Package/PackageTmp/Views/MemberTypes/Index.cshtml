@model IEnumerable<WebApplication2.Models.MemberType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MemType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MemType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.mtypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.mtypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.mtypeId })
        </td>
    </tr>
}

</table>
