@model IEnumerable<WebApplication2.Models.BusinessType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.btype)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.btype)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.btypeid }) |
            @Html.ActionLink("Details", "Details", new { id=item.btypeid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.btypeid })
        </td>
    </tr>
}

</table>
